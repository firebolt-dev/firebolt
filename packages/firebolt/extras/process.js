// if (!globalThis.process) {
//   globalThis.process = {}
// }
// if (!globalThis.process.env) {
//   globalThis.process.env = {}
// }
// if (!globalThis.process.env.NODE_ENV) {
//   // esbuild define will swap this out
//   process.env.NODE_ENV = FIREBOLT_NODE_ENV
// }
